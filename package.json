{
  "name": "stock-portfolio",
  "private": true,
  "scripts": {
    "build": "cd db && npm run build && cd ../bff && npm run build",
    "test": "npm-run-all test:db test:bff",
    "test:db": "cd db && npm test",
    "test:bff": "cd bff && npm test"
  },
  "devDependencies": {
    "@types/sinon-chai": "^4.0.0",
    "@types/supertest": "^6.0.2",
    "npm-run-all": "^4.1.5",
    "sinon-chai": "^4.0.0",
    "supertest": "^7.0.0"
  }
}